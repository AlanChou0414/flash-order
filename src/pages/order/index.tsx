import { Layout } from "@components/layout";
import i18n from "@i18n";

/* Order Index */
const PageMain = () => {
  return (
    <Layout title={i18n.t('ordering')} shoppingItems={10}>
      <></>
    </Layout>
  );
};

export default PageMain;